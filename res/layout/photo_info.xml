<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".UploadPhoto" >

    <ImageView 
        android:id="@+id/thumbnail_photoinfo"
        android:layout_width="75dp"
        android:layout_height="75dp"
        />
    
    <EditText
        android:id="@+id/caption_photoinfo"
        android:layout_below="@id/thumbnail_photoinfo"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_margin="10dp"
        android:hint="write caption..."
        />
    
    <Button
        android:id="@+id/save_photo_db"
        android:layout_below="@id/caption_photoinfo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="upload"
        android:onClick="uploadToServer"
        />

</RelativeLayout>
